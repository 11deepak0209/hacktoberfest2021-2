#include <stdio.h> 
int main()
{   // arrays in C programming 
    int list [4] ;
    int i=0;
    printf("Enter the array values\n");
   for(i=0 ; i<5 ; i++)
   {
      scanf("%d",&list[i]);
       
   }
// for print the values of arrays
for(i=0 ; i<5 ; i++)
   {
      printf("%d \n",list[i]);
       
   }

}
